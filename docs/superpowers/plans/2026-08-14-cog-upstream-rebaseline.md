# Cog Upstream Re-Baseline — Implementation Plan

**Spec:** [`../specs/2026-08-14-cog-upstream-rebaseline-design.md`](../specs/2026-08-14-cog-upstream-rebaseline-design.md)
**Upstream clone:** `/tmp/claude-1000/-workspaces-Synergy/e113ee39-7bdb-4eb3-86fd-c1a1b0d8b398/scratchpad/cog-upstream` @ `86de1eb`

## Work split

| Part | Files | Owner |
|---|---|---|
| A — memory repo migration | `memory/**` | subagent (mechanical) |
| B — `cog.md` + `CLAUDE.md` + domain shims | `.claude/commands/cog.md`, `CLAUDE.md`, 7 domain skills, `_templates/domain.md`, `setup.md` | lead (judgment) |
| C — pipeline & utility skills | 9 upstream-shared skills + `integrate.md` + `esper.md` | subagent, after B |
| D — docs | `docs/cog/`, `docs/synergy-user-guide.md` | lead |

Parts B and C both write into `.claude/commands/` but touch disjoint file sets. C must run after
B so it can reference the conventions `cog.md` establishes.

## Part A — `memory/` repo migration

`memory/` is its own git repo. Purely mechanical; no judgment calls.

1. `mkdir -p memory/{personal,work/trayverify,work/mthr,work/storyfield,work/podcast,work/visibility,work/website}/threads`
   and add `.gitkeep` to each. Seven directories — `cog-meta` is `type: system` and gets none.
   No thread files exist today, so nothing moves.
2. Create `memory/cog-meta/run-log.md`: L0 header on line 1, an explanatory comment, a
   `## Runs` section, and one seed line `- 2026-08-14 /cog-rebaseline: run log created` so the
   first pipeline run scopes to "since today" instead of mining six months.
3. Create `memory/cog-meta/action-items.md`: L0 header, same task format as domain
   `action-items.md`, empty list.
4. Add `<!-- L0: ... -->` as **line 1** to the 12 files missing one:
   `link-index.md`, `glacier/index.md`, `cog-meta/reflect-cursor.md`,
   `cog-meta/foresight-nudge.md`, `cog-meta/briefing-bridge.md`, and the 7 domain `INDEX.md`
   files (`personal`, `work/{trayverify,mthr,storyfield,podcast,visibility,website}`).
   Write a real summary of each file's actual contents, max 80 chars. Do not disturb existing
   content — L0 goes above any title or frontmatter.
5. `memory/domains.yml`: change the `generated by /setup` header comment to `/cog`, and add
   `threads` to each of the 7 content domains' `files:` list. Leave `cog-meta` alone.

Verification: every `.md` under `memory/` (excluding `.git/`) has `<!-- L0:` on line 1.

## Part B — `cog.md`, `CLAUDE.md`, domain shims (lead)

1. Vendor upstream `cog.md`. Apply the one deviation: memory resolves to `$COG_HOME/memory/` if
   set, otherwise `./memory/` relative to the project root — **not** `~/cog/memory/`, which does
   not exist here.
2. Append a Synergy section to `cog.md`: the Esper directory map and the `esper/*` rows of the
   file-edit-pattern table.
3. Rewrite `CLAUDE.md` from upstream's 125-line base plus the spec's delta table. Target ≤160
   lines. Carry forward the container boundary (`SYNERGY_RAW`, `.devcontainer/`) from the
   container-split spec, and the user's persona lines.
4. Rewrite the 7 domain skills as ~25-line shims on upstream's `personal.md` model, preserving
   each domain's trigger list from `memory/domains.yml`.
5. Regenerate `_templates/domain.md` in shim form.
6. Replace `setup.md` with a one-line alias pointing at `/cog`.

## Part C — pipeline & utility skills (subagent, after B)

Update from upstream, re-applying Synergy-specific content each file already carries:
`reflect.md`, `housekeeping.md`, `evolve.md`, `foresight.md`, `scenario.md`, `history.md`,
`explainer.md`, `humanizer.md`, `commit.md`.

Features that must land in the right owner:
- `reflect.md` — consolidation condition pipeline (3 gates + `<!-- promoted: -->` trail), spike
  detection, minimum-data checks, run-log scoping, thread candidates suggested (not auto-created),
  temporal `from:` review. Keep Synergy's session-transcript JSONL notes and `reflect-cursor.md`
  usage — the run log scopes *which files to read*, the cursor tracks *transcript positions*.
  They are not redundant.
- `housekeeping.md` — temporal `until:` sweep, generated per-domain `INDEX.md`, glacier archival,
  link audit.
- All pipeline skills — append `- YYYY-MM-DD /skill: outcome` to `cog-meta/run-log.md` on
  completion.

Two Synergy-only edits in the same pass, to avoid a write conflict with other plans:
- `integrate.md` — change the security gate from `echo $DEVCONTAINER` to `echo "${SYNERGY_RAW:-}"`,
  blocked unless the value is `1`. `DEVCONTAINER=true` is set in every devcontainer on this
  machine, so the current gate passes in general-purpose containers that hold `memory/` and have
  open egress. The block message must name the fix: run from the Synergy raw-data container
  (`.devcontainer/`).
- `esper.md` — delete `### Fixing — what the CLI handles vs. what you do` and
  `### Auto-fix protocol`; replace with a short section saying `source_count` mismatches, duplicate
  `## Sources` entries, and index drift are hand-edited with `Edit`. Remove `fix` and `add-source`
  from the tooling table, add `check --detail CHECK` / `--limit N`. Remove the clean-git-tree
  caveat. Keep the `cursor.kind: opaque` caveat.

## Part D — docs (lead)

- Delete `docs/cog/*.md` (6 files). Write `docs/cog/UPSTREAM.md` recording
  <https://lab.puga.com.br/cog/>, the fork point `86de1eb`, and the `./memory/` deviation.
- `docs/synergy-user-guide.md`: skill table (`/setup` → `/cog`), cadence (weekly
  `housekeeping → reflect` in one session, monthly `evolve`), stale `~/Documents/Synergy` paths,
  and the Security section — it currently claims `/integrate` has "no network access, no shell
  commands," which is false.

## Verification

- `wc -l CLAUDE.md` ≤ 160.
- Every skill in the `CLAUDE.md` routing table has a file in `.claude/commands/`, and vice versa.
- `grep -rn "~/cog/memory\|docs/cog/architecture\|clean_room\|/setup" CLAUDE.md .claude/commands/ docs/synergy-user-guide.md`
  returns nothing (bar the `/setup` alias file itself).
- Every `.md` under `memory/` has `<!-- L0:` on line 1.
- `grep -rn "DEVCONTAINER" .claude/commands/integrate.md` shows only the new `SYNERGY_RAW` gate.
- Fresh-session smoke test: `/personal` activates and loads domain memory; `/esper` answers a
  query; `/reflect` scopes correctly or declines on thin data.
