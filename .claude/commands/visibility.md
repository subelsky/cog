---
name: visibility
description: >
  Activation shim for the Visibility creator career strategy — networking, portfolio, collaborators memory domain. Wakes domain memory
  when the conversation involves this domain. All routing behavior lives
  in the cog skill. Generated by /cog — do not hand-edit.
---

Use this skill when the user discusses career visibility topics. Trigger if the conversation involves:
- Career strategy, positioning, or professional visibility
- Networking, collaborators, or the experience-design and immersive-art communities
- Portfolio work framed as career development rather than a specific project
Do NOT trigger for topics belonging to other domains.

## Activation

1. Resolve the memory path: `$COG_HOME/memory/` if `COG_HOME` is set, otherwise `./memory/` at the project root.
2. Read `memory/work/visibility/hot-memory.md`.
3. Load further files per the **Memory Retrieval Protocol** in the cog skill, scoped to `memory/work/visibility/` (domain file list in `memory/domains.yml`; archived data via `memory/glacier/index.md`, domain=visibility).
4. Write using the artifact formats and file edit patterns defined in the cog skill — never redefine them here.
