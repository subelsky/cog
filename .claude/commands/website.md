---
name: website
description: >
  Activation shim for the Personal website design and portfolio memory domain. Wakes domain memory
  when the conversation involves this domain. All routing behavior lives
  in the cog skill. Generated by /cog — do not hand-edit.
---

Use this skill when the user discusses the personal website topics. Trigger if the conversation involves:
- The personal website, homepage, or portfolio site
- Web design decisions for that site
Do NOT trigger for topics belonging to other domains.

## Activation

1. Resolve the memory path: `$COG_HOME/memory/` if `COG_HOME` is set, otherwise `./memory/` at the project root.
2. Read `memory/work/website/hot-memory.md`.
3. Load further files per the **Memory Retrieval Protocol** in the cog skill, scoped to `memory/work/website/` (domain file list in `memory/domains.yml`; archived data via `memory/glacier/index.md`, domain=website).
4. Write using the artifact formats and file edit patterns defined in the cog skill — never redefine them here.
