---
name: setup
description: >
  Deprecated alias for /cog. Memory conventions, setup, and domain bootstrap
  all live in the cog skill now.
---

`/setup` was merged into `/cog` upstream. Invoke the **cog** skill instead — it holds the memory
conventions and the bootstrap flow.
